Lista: agrupador de elementos

    Atributos: tamanho da lista, estado da lista, ordenação, último elemento da lista
    Atualizar lista a cada operação de inserção realizada

    Lista linear: armazenamento de dados em memória -> forma não contínua
        Elementos organizados de forma sequencial, mas não contínuo
        Ordem de acesso criada com o encadeamento dos ponteiros -> sequência de ponteiros
        L(i-1) < L < L(i+1) -> para add o elemento 9, o 10 e 11 tem que existir
        LSE -> só tem uma direção de encaminhamento 